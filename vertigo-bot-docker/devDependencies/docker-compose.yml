# docker-compose example file
#
# Passwords / API key can be, for example, generated locally with :
#   < /dev/urandom tr -dc _A-Z-a-z-0-9 | head -c${1:-32};echo;

name: chatbotfactory

services:
  # apache:
    # image: httpd:latest
    # volumes:
      # - ./httpd.conf:/usr/local/apache2/conf/httpd.conf
    # ports:
      # - "8000:80"

  clamav:
    image: clamav/clamav
    ports:
      - "3310:3310"
    networks:
      - cb_factory
    volumes:
      - ./data/clamav/clamd-1.5.2.conf:/etc/clamav/clamd.conf:ro
    healthcheck:
      test: ["CMD", "clamdscan", "--ping", "1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 120s
    stop_grace_period: 30s

#  runnerDev:
#    image: registry.kleegroup.com/chatbot-factory/bot-runner-all-included:3.2.1
#    environment:
#      - DESIGNER_URL=http://localhost:8080/vertigo-bot-designer/
#      - ANALYTICA_HOST=analytica
#      - ANALYTICA_PORT=4562
#      - ANALYTICA_DBNAME=chatbot/autogen
#      - API_KEY=runnerDev
#      - RASA_URL=http://rasa:5005
#      - FRAME_ANCESTORS=*
#      - MAIL_NAME=KleeMail
#      - MAIL_PROTOCOL=smtp
#      - MAIL_HOST=localdelivery.klee.lan.net
#      - MAIL_PORT=25
#      - JAVA_OPTS=-Xmx512m -Xms512m
#      - ENCRYPTION_PASSWORD=ChatbotKlee2022!
#      - CONFIG_DATA_FILE=/opt/data/runnerConfig
#      - CLAMAV_HOST=clamav
#    ports:
#      - '8081:8080'
#    volumes:
#      - cf_runner_dev_data:/opt/data
#    depends_on:
#      rasa:
#        condition: service_healthy
#      analytica:
#        condition: service_started
#      clamav:
#        condition: service_healthy
#      keycloak:
#        condition: service_healthy
#    networks:
#      - cb_factory


  postgres:
    image: postgres:18-alpine
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_PASSWORD: postgres
    volumes:
      - cf_postgres_18_data:/var/lib/postgresql/data/pgdata
      - ./initSql/:/docker-entrypoint-initdb.d/
      - ./restore/:/restore/
    ports:
      - "5433:5432"
    networks:
      - cb_factory
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    stop_grace_period: 30s

  postgresKC:
    image: postgres:18-alpine
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
      POSTGRES_PASSWORD: keycloak
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
    volumes:
      - cf_postgresKC_18_data:/var/lib/postgresql/data/pgdata
    ports:
      - "5434:5432"
    networks:
      - cb_factory
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U keycloak"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    stop_grace_period: 30s

  keycloak:
    image: keycloak/keycloak:latest
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: changeme
      KC_DB_URL_HOST: postgresKC
      KC_DB_URL_PORT: 5432
      KC_DB_URL_DATABASE: keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_SCHEMA: public
      KC_DB_PASSWORD: keycloak
      KC_DB: postgres
      KC_HOSTNAME_STRICT: "false"
      KC_HTTP_ENABLED: "true"
      KC_HEALTH_ENABLED: "true"
      KC_METRICS_ENABLED: "true"
      KC_LOG_LEVEL: INFO
      ##,org.keycloak.connections:DEBUG,org.keycloak.transaction:DEBUG,org.keycloak.services:DEBUG,org.keycloak.authentication:DEBUG
    ports:
      - "8200:8080"
    entrypoint:
      - /opt/keycloak/bin/kc.sh
      - start
      - --verbose
      - --import-realm
      - --features-disabled=impersonation
#      - --optimized
    volumes:
      - ./data/keycloak/import:/opt/keycloak/data/import
    depends_on:
      postgresKC:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/9000 && echo -e \"GET /health/ready HTTP/1.0\\r\\nHost: localhost\\r\\n\\r\\n\" >&3 && cat <&3 | grep -q UP'"]
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 120s
    networks:
      - cb_factory
    stop_grace_period: 30s

  influxdb:
    image: influxdb:alpine
    volumes:
      #- cf_influxdb_data:/var/lib/influxdb
      - cf_influxdb2_data:/var/lib/influxdb2
      - cf_influxdb2_config:/etc/influxdb2
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      #DOCKER_INFLUXDB_INIT_MODE: upgrade
      DOCKER_INFLUXDB_INIT_USERNAME: admin
      DOCKER_INFLUXDB_INIT_PASSWORD: changeme
      DOCKER_INFLUXDB_INIT_ORG: chatbot
      DOCKER_INFLUXDB_INIT_BUCKET: admin
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: influxDevToken
    ports:
      - "8086:8086"
    networks:
      - cb_factory
    stop_grace_period: 30s

  chronograph:
    # IHM de consultation influxDB
    # Note: la variante "1.11.1-alpine" est cassee chez InfluxData (binaire
    # /usr/bin/chronograf place par erreur dans /usr/bin/usr/bin/chronograf).
    # On reste sur l'image Debian "1.11.1" tant que la 1.11.x-alpine n'est
    # pas corrigee. Voir https://hub.docker.com/_/chronograf
    image: chronograf:alpine
    volumes:
      - cf_chronograph_data:/var/lib/chronograf
    ports:
      - "8888:8888"
    networks:
      - cb_factory

  analytica:
    image: vertigoio/vertigo-analytics-server:0.14.0
    environment:
      INFLUXDB_URL: http://influxdb:8086
      INFLUXDB_ORG: chatbot
      INFLUXDB_TOKEN: influxDevToken
    depends_on:
      - influxdb
    ports:
      - "4562:4562"
    networks:
      - cb_factory
    stop_grace_period: 30s

  rasa:
    image: rasa/rasa:main-full
    environment:
      - LOG_LEVEL=DEBUG
      # Force UTF-8 everywhere (locale + Python UTF-8 mode) so that
      # randomly-generated model file names containing non-ASCII characters
      # (e.g. nlu-...-sticky-rosé.tar.gz) are encoded/decoded consistently
      # between the train step and the agent reload step. Without this the
      # reload fails with HTTP 400 because the file name comes back mojibaked
      # (rosé -> rosÃ©).
      - LANG=C.UTF-8
      - LC_ALL=C.UTF-8
      - PYTHONUTF8=1
    ports:
      - "5005:5005"
    command:
      - run
      - --enable-api
      - --cors
      - "*"
    networks:
      - cb_factory
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5005/ || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 60s
    stop_grace_period: 30s

volumes:
  cf_postgres_18_data:
  cf_postgresKC_18_data:
#  cf_influxdb_data:
  cf_influxdb2_data:
  cf_influxdb2_config:
#  cf_rabbitmq_data:
  cf_chronograph_data:
#  cf_runner_dev_data:

networks:
  cb_factory:
